import { DigitDirective } from './digit.directive';

describe('DigitDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitDirective();
    expect(directive).toBeTruthy();
  });
});
